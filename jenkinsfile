pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
              git branch: 'main', url: 'git@github.com:manubolusivananda/TERRAFORM_CICD.git'
            }
        }
        stage('init') {
            steps {
                sh 'terraform init'
            }
        }
         stage('plan') {
            steps {
                sh 'terraform plan'
            }
        }
    }
}
